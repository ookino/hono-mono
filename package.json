{
  "name": "starter",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun run --filter '@workspace/api' dev & bun run --filter 'web' dev",
    "check": "biome check --write .",
    "format": "biome format --write ."
  }
}
